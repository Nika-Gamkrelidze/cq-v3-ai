ai.communiq.ge {
    encode gzip

    handle_path /api/* {
        reverse_proxy api:8000
    }

    handle /deploy* {
        reverse_proxy host.docker.internal:9000
    }

    handle {
        root * /srv
        try_files {path} /index.html
        file_server
    }
}
